"""Primus agents package (Phase 6) — re-exports the agent/orchestration API."""
from .system import (  # noqa: F401
    AGENT_OUTPUTS_DIR,
    BackgroundAgentManager,
    BackgroundTaskManager,
    DynamicModelManager,
    ModelRouter,
    ScheduledTaskManager,
    _BG_TASK_ICONS,
    _WEEKDAYS,
    _fast_model_available,
    _response_path_label,
    _schedule_label,
    acknowledgment_text,
    append_followup,
    build_thought_messages,
    classify_long_task,
    coerce_message_text,
    enforce_sequential_models,
    gradio_history_to_messages,
    handle_meta_instruction,
    init_agent_graphs,
    invoke_primus,
    is_meta_instruction,
    list_agent_outputs,
    make_chat_ollama,
    polish_response,
    render_background_agents_html,
    render_background_tasks_md,
    render_recent_outputs_md,
    render_scheduled_tasks_md,
    start_turn_watchdog,
)
